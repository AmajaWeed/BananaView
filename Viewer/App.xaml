<Application x:Class="Viewer.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Viewer"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <local:BoolToHighlightConverter x:Key="BoolToHighlightConverter"/>

        <Style x:Key="ToolbarButton" TargetType="Button">
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="#E6E6E6"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="24"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Margin" Value="1,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg" Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#33FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#55FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TitleBarButton" TargetType="Button" BasedOn="{StaticResource ToolbarButton}">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Width" Value="59"/>
            <Setter Property="Height" Value="43"/>
            <Setter Property="Margin" Value="0"/>
        </Style>

        <!-- Play/pause - 33% bigger than the base toolbar button -->
        <Style x:Key="NavToolbarButton" TargetType="Button" BasedOn="{StaticResource ToolbarButton}">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
        </Style>

        <!-- Prev/next - 2x the base toolbar button -->
        <Style x:Key="BigNavToolbarButton" TargetType="Button" BasedOn="{StaticResource ToolbarButton}">
            <Setter Property="FontSize" Value="21"/>
            <Setter Property="Width" Value="48"/>
            <Setter Property="Height" Value="48"/>
        </Style>

        <Style x:Key="DeleteToolbarButton" TargetType="Button" BasedOn="{StaticResource ToolbarButton}">
            <Setter Property="Foreground" Value="#FF5C5C"/>
        </Style>

        <Style x:Key="ToolbarSeparator" TargetType="Separator">
            <Setter Property="Width" Value="1"/>
            <Setter Property="Height" Value="13"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Background" Value="#33FFFFFF"/>
        </Style>
    </Application.Resources>
</Application>
